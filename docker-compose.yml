version: "3.9"

services:
  career-agent:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/jobs.db
    restart: unless-stopped
